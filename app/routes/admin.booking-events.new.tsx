import { env } from "cloudflare:workers";
import { data, Form, Link, redirect } from "react-router";
import type { Route } from "./+types/admin.booking-events.new";
import { requireRole } from "../services/auth.server";
import { Notice, PageIntro, PageShell } from "../components/design-system";
const runtime = env as unknown as { AUTH_SECRET?: string; BETTER_AUTH_URL?: string };
export async function action({ request }: Route.ActionArgs) { await requireRole(request, env.trice_auction_db, runtime, "admin"); const form = await request.formData(); const result = await saveEvent(form); if (!result.ok) return data(result, {status:400}); const event = await env.trice_auction_db.prepare("INSERT INTO booking_events (name, description, opens_at, closes_at, timezone, active, admin_notes) VALUES (?, ?, ?, ?, ?, ?, ?) RETURNING id").bind(result.value.name, result.value.description, result.value.opensAt, result.value.closesAt, result.value.timezone, result.value.active, result.value.adminNotes).first<{id:number}>(); return redirect(`/admin/booking-events/${event!.id}`); }
export default function NewBookingEvent({ actionData }: Route.ComponentProps) { return <PageShell><div className="max-w-3xl"><Link to="/admin/booking-events" className="font-bold text-[#9d302f] underline">← Booking Events</Link><PageIntro eyebrow="Trice Auctions · Administration" title="New Booking Event">Save the customer-facing signup window first, then add Drop-Off Dates with their own capacities.</PageIntro>{actionData && !actionData.ok ? <Notice variant="error">{actionData.errors.join(" ")}</Notice> : null}<BookingEventForm /></div></PageShell>; }
export function BookingEventForm({ event, submitLabel = "Create Booking Event" }: { event?: {name:string;description:string|null;opensAt:string;closesAt:string|null;timezone:string;active:boolean;adminNotes:string|null}; submitLabel?:string }) { return <Form method="post" className="grid gap-5 rounded-xl border border-[#dfe1e4] bg-white p-6"><label className="ta-field">Name / title<input required name="name" defaultValue={event?.name ?? ""}/></label><label className="ta-field">Customer-facing description<textarea name="description" defaultValue={event?.description ?? ""} className="min-h-28"/></label><div className="grid gap-5 sm:grid-cols-2"><label className="ta-field">Signup opens<input required name="opensAt" type="datetime-local" defaultValue={toLocalInput(event?.opensAt)}/></label><label className="ta-field">Signup closes <span className="font-normal">(optional)</span><input name="closesAt" type="datetime-local" defaultValue={toLocalInput(event?.closesAt)}/></label></div><label className="ta-field">Timezone<input required name="timezone" defaultValue={event?.timezone ?? "America/New_York"}/></label><label className="ta-field">Internal admin notes <span className="font-normal">(optional)</span><textarea name="adminNotes" defaultValue={event?.adminNotes ?? ""}/></label><label className="font-semibold"><input className="mr-2" name="active" type="checkbox" defaultChecked={event?.active ?? true}/>Active for public signup</label><button className="ta-button ta-button-primary">{submitLabel}</button></Form>; }
export async function saveEvent(form: FormData) { const name=String(form.get("name")||"").trim(), opens=String(form.get("opensAt")||""), closes=String(form.get("closesAt")||""), timezone=String(form.get("timezone")||"America/New_York").trim(); const errors:string[]=[]; if(!name) errors.push("Name is required."); if(!opens || Number.isNaN(Date.parse(opens))) errors.push("A valid signup opening date and time is required."); if(closes && Number.isNaN(Date.parse(closes))) errors.push("The closing date and time is invalid."); const opensAt=opens ? new Date(opens).toISOString() : "", closesAt=closes ? new Date(closes).toISOString() : null; if(opensAt && closesAt && closesAt <= opensAt) errors.push("Signup closing must be after signup opening."); if(!timezone) errors.push("Timezone is required."); return errors.length ? {ok:false as const,errors} : {ok:true as const,value:{name,description:String(form.get("description")||"").trim()||null,opensAt,closesAt,timezone,active:form.has("active")?1:0,adminNotes:String(form.get("adminNotes")||"").trim()||null}}; }
function toLocalInput(value?: string | null) { return value ? value.slice(0,16) : ""; }
